#!/usr/bin/env node
'use strict';

var _Builder = require('./generators/Builder.js');

var _Builder2 = _interopRequireDefault(_Builder);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var name = process.argv[3];

switch (process.argv[2]) {
    case 'init':
        var config = new _Builder2.default('init', 'config');
        console.log(config.build());
        break;
    case 'make:page':
        console.log(name);
        var page = new _Builder2.default(name, 'page');
        console.log(page.build());

        break;
    case 'make:action':
        var action = new _Builder2.default(name, 'action');
        console.log(action.build());

        break;
    case 'make:reducer':
        var reducer = new _Builder2.default(name, 'reducer');
        console.log(action.build());

        break;
    default:
        console.log('Command not found. \n Giffy version: 1.0');
}